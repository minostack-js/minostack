# Vast Review — `minostack-js` Monorepo (All Packages)

**Date:** 2026-09-08T15:17:00Z  
**Mode:** read-only, no mutations  
**Packages in scope:** `schema`, `openapi`, `graphql`, `mino`, `kernel`, `runtime-node`, `runtime-bun`, `runtime-deno` + `tooling/*`, `apps/*`, `playground`, `examples`  
**Verification ran:** `pnpm check` (format:check → lint → typecheck → test) **16/16 PASS** (after threshold overrides), `pnpm build` **9/9**, `pack --dry-run` tarball leakage guard **PASS**  
**Tooling base:** `tooling/tsconfig/base.json: ES2022, strict:true, NodeNext, verbatimModuleSyntax` + `tooling/vitest-config/base.js: 90% gate` + `tooling/eslint-config/base.js: oxlint correctness → typescript-eslint → prettier last`

> **Executive verdict: A- (strong foundation, enterprise-ready direction, but P0 HIGH bugs in `kernel` exports / `mino` router `decode`/`prefix` / `runtime-node` `set-cookie` must be fixed before v0.1 publish).** No critical data-loss-without-warning except `runtime-node` cookie corruption. Overall architecture faithfully implements `prompt.md` §4 dependency discipline and `AGENTS.md` §4 ESM/zero-deps/portable rules.

---

## 0. Overall Repo Health — `pnpm-workspace.yaml`, `turbo.json`, `package.json`, `tooling`

| Area                                                                                                                                                            | Status                   | Evidence                                                                                                                                                                                                                                                      |
| --------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Workspace** `apps/*`, `packages/*`, `tooling/*`, `playground`, `examples` + `catalog:` pinning                                                                | ✅                       | `pnpm-workspace.yaml:1-7,10-26` `catalog: typescript ^6.0.3` (correctly held vs TS 7, `docs/architecture.md:47-50`)                                                                                                                                           |
| **Turbo** `build.dependsOn:["^build"]` `outputs:["dist/**"]`                                                                                                    | ✅                       | `turbo.json:5` respects `kernel→mino` build order; `test→^build` where needed                                                                                                                                                                                 |
| **TypeScript** `base.json: target ES2022, strict, noUncheckedIndexedAccess, NodeNext, verbatimModuleSyntax` + `library.json: stripInternal:true declarationMap` | ✅                       | `tooling/tsconfig/base.json:2-16`, `tooling/tsconfig/library.json:1-9`; all `packages/*/tsconfig.json` extend `base` + `tsconfig.build.json` extend `library`                                                                                                 |
| **Lint** `oxlint` correctness first + `eslint-plugin-oxlint` + `eslint-config-prettier` last                                                                    | ✅                       | `tooling/eslint-config/base.js:1-32`; per-package `eslint.config.mjs: ...base` + `no-restricted-imports node:*` on `src/**/*.ts` for `schema` (should be all `src`)                                                                                           |
| **Test** `vitest` `defineWorkspace()` `V8` 90% gate                                                                                                             | ⚠️ **HIDDEN DIVERGENCE** | `tooling/vitest-config/base.js:31-36` 90% lines/branches/functions/statements, but `packages/mino/vitest.config.ts:4` overrides to 85/70/90/85, `kernel:50`, `runtime-*:60` to hide gaps — violates `AGENTS.md §9 Done means 90%` (see per-package coverage). |
| **CI** `check` → `build` + new `pack --dry-run` tarball guard                                                                                                   | ✅                       | `.github/workflows/ci.yml:36-48` now asserts `mino` no `kernel/openapi/graphql`, `runtime-node` no `kernel`                                                                                                                                                   |
| **Security** `.gitignore: node_modules/dist/.turbo/coverage/.env`                                                                                               | ✅                       | No `dist`/`lock` hand-edits, `pnpm-lock.yaml` committed                                                                                                                                                                                                       |
| **Releases** `Changesets` independent, `prepublishOnly: build`                                                                                                  | ✅                       | All `packages/*/package.json: prepublishOnly`                                                                                                                                                                                                                 |

**Cross-cutting risk (HIGH):** Lowered thresholds mask real gaps — `mino` 73% branches, `kernel` 55% branches, `runtime-node` 65% branches would **fail** 90% gate. Must restore or document exclusion per `base.js` `exclude` param.

---

## 1. `@minostack/schema` — `packages/schema` — **A (exemplary)**

**Entry:** `src/index.ts: m` facade (`private constructor`, static `m.string()`…`m.lazy()`), `package.json:7-11` `exports: {".":{types,import}}`, `sideEffects:false`, zero runtime deps.

| Dimension                                                                                                                                                           | Verdict | Key Evidence                                                                                                                  |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------- | ----------------------------------------------------------------------------------------------------------------------------- |
| **Architecture** canonical `SchemaNode` + interpreter                                                                                                               | ✅      | `src/core/node.ts:1-282` union, `src/core/validate.ts:780-913` dispatch, ESM cycle avoidance comment `src/core/schema.ts:1-9` |
| **API** `string/number/boolean/bigint/date/literal/enum/object/array/tuple/record/union/intersection/optional/nullable/lazy + transform/refine/describe/meta/facet` | ✅      | `src/index.ts:48-158`, no `coerce`/`compile`/`parseAsync` (correctly deferred)                                                |
| **Immutability** every modifier `withNode({…spread})`                                                                                                               | ✅      | `src/primitives/string.ts:14`, `src/core/schema.ts:175-180` `facet` shallow copy documented; test `matrix.test.ts:33-42`      |
| **Validation** deterministic, no silent coercion, unicode `[...value].length`                                                                                       | ✅      | `validate.ts:56-58`, `242-249` rejecting `"42"` for `number`                                                                  |
| **Type inference** `m.infer` + `InputOf/OutputOf` + phantom `~input`                                                                                                | ✅      | `src/core/schema.ts:53-60`, `src/index.ts:163-171`, `test/types.test.ts:34`                                                   |
| **Errors** stable `code/path/expected/received`, `ValidationError`                                                                                                  | ✅      | `src/core/errors.ts:9-19` `invalid_type/invalid_literal/too_small/.../custom`, `validate.ts:47-49` `child(path,key)`          |
| **Prototype safety** `setKey` `defineProperty` for `__proto__`                                                                                                      | ✅      | `src/core/utils.ts:16-27` + `test/edges.test.ts:159-175`                                                                      |
| **Coverage** 99.16% stmts, 95.93% branches (76 tests)                                                                                                               | ✅      | `tooling/vitest-config/base.js:31` gate pass                                                                                  |

**Top findings (1 HIGH, 3 MEDIUM, rest LOW):**

| Sev        | File:Lines                                                       | Issue                                                                                                                                                                                                       |
| ---------- | ---------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **HIGH**   | `src/core/validate.ts:885-908`                                   | `transform`/`refine` throwing escapes as raw error, not `ValidationError` — `try {schema.parse(x)} catch(e) if instanceof ValidationError` fails. Wrap in `try/catch → custom` issue.                       |
| **MEDIUM** | `src/core/validate.ts:429-437`                                   | `copyDefault` shallow — `m.object({cfg:m.object({a:m.number()}).default({a:1,nested:{b:2}})})` shares `nested` reference across `parse({})` calls. Use `structuredClone` or document shallow.               |
| **MEDIUM** | `src/core/validate.ts:94-104` & `src/primitives/string.ts:14-77` | String `trim` order-sensitive: `m.string().min(2).trim()` checks min on untrimmed, then trims. Only `trim().min(2)` tested. Document “order matters” or enforce normalizers first.                          |
| **MEDIUM** | `src/composites/object.ts:19-26,118-134`                         | `required()` unwraps only `OptionalSchema`/`DefaultSchema`, not `union(string,undefined)` — `undefined extends Output` makes key optional at type level but runtime `required()` keeps union. Inconsistent. |
| **LOW**    | `src/core/validate.ts:56-58`                                     | `stringLength` allocates `[...value]` per check → 3× for 3 checks. Cache `len`.                                                                                                                             |
| **LOW**    | `src/core/validate.ts:574-575`                                   | `validateTuple` `slice(0,count)` copy per validate.                                                                                                                                                         |
| **LOW**    | `src/composites/record.ts:65-75`                                 | `record(values)` vs `record(keys,values)` arity via `values===undefined` weak.                                                                                                                              |
| **LOW**    | `src/core/validate.ts:675-698`                                   | Union returns `invalid_union` + all variant issues (verbose but useful). Document.                                                                                                                          |

**Grade:** **A** — blueprint-faithful, production-ready for v0.1; fix HIGH `transform` throw + deep-clone docs.

**Paths:** `packages/schema/src/core/node.ts`, `src/core/validate.ts`, `src/core/schema.ts`, `src/composites/*.ts`, `src/primitives/*.ts`, `package.json`, `tooling/tsconfig/*`

---

## 2. `@minostack/openapi` — `packages/openapi` — **A- (enterprise-ready, 2 HIGH in Mino integration)**

**Core:** One parameterized engine `convertInner(node,ctx,path)` + `ctx.version` lowering (`src/oas.ts:521-823`).

| Kind                                                                               | Mapping                                                                                     | Evidence                                   |
| ---------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------- | ------------------------------------------ |
| string `min/max/length/regex/startsWith/endsWith/includes/email/url/uuid/datetime` | `minLength/maxLength/pattern/format`                                                        | `src/oas.ts:524-574`, `escapeRegExp 72-74` |
| number `int/safe→integer`, bounds                                                  | `exclusiveMinimum` (3.1) vs `minimum+exclusiveMinimum:true` (3.0) via `applyBounds 496-519` | `oas31.test.ts:49-57`                      |
| bigint `int64` + `isSafeNumber`                                                    | `bigint-bound-omitted` warn                                                                 | `src/oas.ts:613-682`                       |
| literal `const` (3.1) vs `enum:[]` (3.0)                                           | `src/oas.ts:687-724`                                                                        |
| object `required` via `acceptsUndefined 366-396`                                   | `src/oas.ts:827-845`                                                                        |
| array/tuple/record/union/discriminatedUnion/intersection/wrappers                  | All 25 `SchemaKind` covered                                                                 | `src/oas.ts:745-823`                       |
| Nullable 3.0 `$ref`+`nullable:true` via `allOf`                                    | `applyNullable 483-494`                                                                     | `edges.test.ts:105-116`                    |
| Metadata `title/description/deprecated/examples→examples/example, default`         | `applyMeta 423-462` + `toJson/defaultValue` JSON-safe                                       | `oas31.test.ts:119-134`                    |

**Warnings (11 codes, stable `code+path`):** `normalizer-dropped`, `bigint-bound-omitted`, `undefined-literal`, `tuple-positions-lost`, `property-names-dropped`, `nonserializable-default/example`, `transform-input-side`, `refinement-dropped`, `webhooks-requires-3.1`, `jsonSchemaDialect-requires-3.1` — all with `warn(ctx,path,code,msg)` `64-66` and component-prefixed in documents.

**Findings (core):**

| Sev               | File:Lines                                                                                                                                                                                          | Issue                                                                                                                                                                                                                     |
| ----------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **MEDIUM**        | `src/oas.ts:553-561`                                                                                                                                                                                | Multiple `pattern` (`startsWith+endsWith`) overwrites — `m.string().startsWith("a").endsWith("b") → "b$"` loses `^a`. Should combine or warn.                                                                             |
| **LOW**           | `src/oas.ts:551`                                                                                                                                                                                    | `pattern: check.pattern.source` drops regex flags (`/a/i`).                                                                                                                                                               |
| **LOW**           | `src/oas.ts:366`                                                                                                                                                                                    | `acceptsUndefined` misses `intersection` → required incorrectly.                                                                                                                                                          |
| **HIGH (Mino)**   | `src/mino.ts:95-122`                                                                                                                                                                                | `autoCounter` dead, `autoSchemas` gates only `input` not `output`, `collectedSchemas` never populated, `requestBody` merge overwrites existing `content` — should merge `...existing.content, "application/json":schema`. |
| **MEDIUM (Mino)** | `src/mino.ts:116-121`                                                                                                                                                                               | Same merge bug.                                                                                                                                                                                                           |
| **MEDIUM**        | `src/oas.ts:340-343` `convertComponents` `headers/securitySchemes` passed raw, not `setProp` per-key — `__proto__` via header name bypasses sanitization (vs `responses/parameters` use `setProp`). | `src/oas.ts:77-88` sanitizes `__proto__` via `defineProperty` elsewhere.                                                                                                                                                  |

**Mino Integration (`src/mino.ts:89-163`):** Duck-typed `MinoApp {getRoutes(): {method,path}[]}` — **no** `import {Mino}` runtime, only `type {Schema}` (erasable). `minoPathToOpenApi :param→{param}, *→{wildcard}`, `getRouteMapEntry` supports `Map|Record` + case-insensitive method. 7 tests (`test/mino.test.ts`) covering `GET /:id`, `POST` input/output, `Map`, wildcard, version 3.0, existing `paths` merge. Coverage 97.5% stmts, 94% branches (gate PASS after adding tests; previously 86% branches would fail).

**Grade:** **A-** — 100% kind coverage, correct 3.0 lowering, hostile-key safe where applied. Fix Mino dead `autoCounter` + `requestBody` merge + header sanitization before publish.

**Paths:** `packages/openapi/src/oas.ts`, `src/types.ts`, `src/mino.ts`, `src/index.ts`, `test/*.test.ts`, `package.json`

---

## 3. `@minostack/graphql` — `packages/graphql` — **A- (SDL complete, 3 MEDIUM docs/incidental)**

**Design:** Every `object` → `type X` + `input XInput`; `Query/Mutation/Subscription` → `type` only (reserving `*Input`); `enum` → `enum` with sanitized members; `union` → `union X = A|B`; `sdl` deterministic via `claimName`+`sanitizeName`+`pascalField`.

| Area                                                                                                               | Evidence                                                                                  | Verdict                                                      |
| ------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------- | ------------------------------------------------------------ |
| Nullability `inputUndef/outputUndef/nullAccepting/nonNull` cycle-safe                                              | `src/sdl.ts:184-293` with `Set` polarity                                                  | ✅                                                           |
| Scalars `BigInt/DateTime/JSON` dedup                                                                               | `src/sdl.ts:1158-1162`                                                                    | ✅                                                           |
| Enums (dedup `a-b`/`a_b→a_b2`, sanitized `_1x`)                                                                    | `src/sdl.ts:355-392` + `test/coverage.test.ts:26-38`                                      | ✅                                                           |
| Unions (input→JSON, output filters `null/undefined`)                                                               | `src/sdl.ts:637-705`                                                                      | ✅                                                           |
| Intersections (object+object merge + warn, else JSON)                                                              | `src/sdl.ts:737-781` (synthetic `{metadata:{},facets:{}}` drops description — **MEDIUM**) | ⚠️                                                           |
| Federation `facet("federation",{key,shareable,extends,tags,override})` → `extend schema @link`/`@key`/`@shareable` | `src/sdl.ts:115-181`, `test/enterprise.test.ts:116-130`                                   | ✅                                                           |
| Field args `facet("field",{args:{id:m.string()}})` on operation roots                                              | `src/sdl.ts:414-455`, `test/enterprise.test.ts:132-150`                                   | ✅                                                           |
| `transform` input exact, output JSON `transform-output-opaque`                                                     | `src/sdl.ts:572-583`                                                                      | ✅                                                           |
| Number bounds silent (no `number-bounds-dropped` warn)                                                             | `src/sdl.ts:479-482` `Int`/`Float` only                                                   | **MEDIUM** — silent loss vs `normalizer-dropped` for strings |
| `generateMinoSDL` thin alias `sdl(types,{query,mapping...})`                                                       | `src/mino.ts:12` no `mino` dep, `...options` overrides `mapping` silently                 | **LOW** — precedence undocumented                            |

**Warnings (17 codes):** `transform-output-opaque`, `refinement-dropped`, `normalizer-dropped`, `tuple-as-json`, `record-as-json`, `scalar-union-as-json`, `input-union-as-json`, `intersection-merged/as-json`, `nonserializable-default`, `name-sanitized`, `literal-fallback`, `required-nullable`, `root-kind-unsupported`, `duplicate-type` — plus `invalid-operation-root/field` (2) omitted from README ( **MEDIUM** docs drift).

**Coverage:** 98.56% stmts, 91.2% branches, 100% funcs (50 tests). `vitest.config.ts` should explicitly `defineWorkspace({exclude:["src/types.ts"]})` with justification (currently passes v8 omits `types.ts` silently).

**Grade:** **A-** — valid Apollo v2 subgraph SDL, hostile-key safe (`setField` `defineProperty` for `__proto__`), deterministic names. Fix nested intersection facets + `README` code list + document number-bounds silence.

**Paths:** `packages/graphql/src/sdl.ts`, `src/types.ts`, `src/mino.ts`, `test/*.test.ts`, `package.json`

---

## 4. `@minostack/mino` — `packages/mino` — **B+ (correct, but 3 HIGH router/client/validator bugs)**

**Public surface:** `Mino`/`Context`/`Router`/`compose`/`validator`/`dto`/`createSSEStream`/`formatSSE`/`createClient`/`hc`/`defineRoute` + types `Handler`, `ParamsFor`.

### 4.1 Router `src/router.ts:1-178` — radix-inspired, correctness-first

| Sev        | Lines                               | Issue                                                                                                                                                                                                           |
| ---------- | ----------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **HIGH**   | `router.ts:144,154`                 | `decodeURIComponent(seg)` / `segments.slice(idx).map(decodeURIComponent)` **throws `URIError` on `%ZZ` → 500**, not `400`. Wrap `try/catch → return null` or `throw BadRequestError`. No test for `/files/%2G`. |
| **MEDIUM** | `router.ts:51-59`                   | `*` not validated as last segment — `/a/*/b` silently truncates to `/a/*`. Should `throw` if `i !== segments.length-1`.                                                                                         |
| **MEDIUM** | `router.ts:64-69`                   | Single `paramChild` → `/users/:id` then `/users/:name` collides, second param name lost (first writer wins).                                                                                                    |
| **MEDIUM** | `router.ts:91-108`                  | No 405 — path matches but method mismatched returns `null` → `notFound` 404, never `errors.ts:21` `405`. Should return 405 + `Allow`.                                                                           |
| **MEDIUM** | `router.ts:163-177` vs `mino.ts:27` | `MinoOptions.strict` (trailing slash normalized) never read — `Router.splitPath` always strips `/`. `strict:true` is no-op.                                                                                     |
| **LOW**    | `router.ts:80-87`                   | Duplicate `GET /a` concatenates `[...existing,...handlers]` vs last-wins — hides duplicate registration bugs.                                                                                                   |

**Correct:** priority static→param→wildcard via DFS + backtrack restore `prev` (`router.ts:135-158`), wildcard `params.wildcard=""` for `/files` (`test/mino.test.ts:47`), `HEAD→GET` fallback `router.ts:99`, `splitPath` query/fragment stripping.

### 4.2 Middleware `src/compose.ts:1-93` + `src/mino.ts:63-105`

| Sev        | Lines                                                     | Issue                                                                                                                                                                                                                |
| ---------- | --------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **HIGH**   | `mino.ts:71`                                              | Path-scoped `app.use("/api",…)` prefix check `!c.path.startsWith(prefix)` → `/api-test` false-positive. Must be `c.path===prefix \|\| c.path.startsWith(prefix+"/")`. Test `test/mino.test.ts:139` misses this case. |
| **MEDIUM** | `mino.ts:70-99` & `compose.ts:25-92`                      | Dual `dispatch` closures per request + `compose(handlers)` called **per-request** (`mino.ts:288`) → **not precompiled** despite comment `compose.ts:1` “precompiled”. Cache per-route would cut allocs.              |
| **MEDIUM** | `compose.ts:68-82`                                        | Auto-continue `if (!nextCalled && i+1<handlers.length) await dispatch(i+1)` violates explicit `next()` — masks middleware ordering bugs, subtle double-dispatch with path wrapper.                                   |
| **MEDIUM** | `compose.ts:10` `instanceof Response` cross-realm fragile | Fails across `node:fetch` polyfill/Workers. Duck-type `typeof v?.status==="number"` safer.                                                                                                                           |
| **LOW**    | `compose.ts:26-27,41`                                     | Double-next messages inconsistent (`"next() called multiple times"` vs `"...in same handler"`), leaks as 500 via `console.error` (`mino.ts:308`).                                                                    |
| **MEDIUM** | Undocumented `HEAD` `content-length` mismatch             | `mino.ts:293-296` strips body but keeps original `content-length` header.                                                                                                                                            |

**Context `src/context.ts:1-301`:** Lazy `#url ??= new URL(req.url)` (55), `#query`/`#queryAll` (89-108) — good. But `Mino.fetch` pre-parses `new URL(request.url).pathname` (261) duplicating parse. `body()` (213-225) allocates **two** `Response` (`res` discarded) and spreads `init.headers` via `...Object.fromEntries(h)` (fails for `Headers` instance). `#headerMap` + `finalizeHeaders` merge queued `headerSet` correctly but undocumented. `status(999)` not range-checked.

### 4.3 Validation `src/validator.ts:1-172` — AnySchema

| Sev        | Lines                  | Issue                                                                                                                                                                                                                                                                                                                                           |
| ---------- | ---------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **HIGH**   | `validator.ts:68-73`   | `await c.req.text()` **without `clone()`** consumes body → downstream `c.jsonBody()` throws `body already used`. Clone used only for non-JSON branch (63) and `form` (77). Should `clone()` + cache parsed value via `setValidated`, and make `jsonBody()` read from cache if present. Comment `validator.ts:163-166` acknowledges empty block. |
| **MEDIUM** | `validator.ts:59-76`   | Inconsistent `clone()` — non-JSON `clone().json()` preserves, JSON `text()` consumes. After failed validation `req.bodyUsed===true` breaks retries.                                                                                                                                                                                             |
| **MEDIUM** | `validator.ts:103-138` | Priority now `~standard → safeParse → parse` (fixed in last patch, previously reverse). Correct but `AnySchema` first checked `safeParse` would shadow `~standard` for dual libs (Zod). Now fixed to `~standard` first as documented.                                                                                                           |
| **LOW**    | `validator.ts:48-53`   | `getTargetValue` dead for `json`/`form` (always `undefined`, branched away in `validator()` 152).                                                                                                                                                                                                                                               |

**Correct:** `AnySchema` supports `@minostack/schema` (`safeParse`), Standard Schema (`~standard`), `parse` fallback; `StandardSchema` type (11-19) correct; `ValidationError` 422.

### 4.4 Client `src/client.ts:1-151` — Proxy RPC

| Sev        | Lines               | Issue                                                                                                                                                                                                                      |
| ---------- | ------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **HIGH**   | `client.ts:84-133`  | Proxy missing `then` trap → `await client` or `Promise.resolve(client)` returns proxy `["then"]`, making client **thenable → hang**. Must `if(prop==="then"\|\|"catch"\|\|"finally") return undefined;` (hono-client bug). |
| **MEDIUM** | `client.ts:62-64`   | `path.replace(`:${k}`, ...)` only **first** `:id` — duplicate param (`/a/:id/b/:id`) leaks. Use `replaceAll` or regex.                                                                                                     |
| **MEDIUM** | `client.ts:59-73`   | `buildUrl` trailing slash strips one `/` only, `path.replace(/\/+/g,"/")` not applied to `baseUrl` with path.                                                                                                              |
| **LOW**    | `client.ts:117-122` | `GET/HEAD` body suppression correct.                                                                                                                                                                                       |

**SSE `src/sse.ts`/`context.ts:265-300`:** `formatSSE` spec-compliant split `data` by `\n`; `createSSEStream` + `TransformStream<string,Uint8Array>` + `TextEncoder` correct. **LOW:** `sse.ts:16` `if(event.retry)` drops `retry:0`; should `!=null`; `context.ts:275-283` `sse()` `AsyncIterable` wrapper without `cancel()` forwarding may leak generator.

**Contract `src/contract.ts:1-94`:** `WeakMap` per handler correct; **LOW** `describeRoute` sets `method:"",path:""` losing identity.

**Errors `src/errors.ts`:** `HttpError` `expose: status<500` correct, `ValidationError` 422 + `issues`, `statusText` map incomplete (no 100/200) — LOW.

**Types `src/types.ts`:** `ParamsFor<Path>` required, not optional for `:id?` (should be `?: string`). **MEDIUM**.

**Performance:** `compose` per-request, `Context` `new Headers()` per request + lazy `URL` + `finalizeHeaders` double copy, `Router` `params:{...params}` copy — contradicts “low allocations” claim. Acceptable for v0.1 correctness but must be noted.

**Package:** Zero runtime deps (`package.json:41` empty, `_comment` dev-only), `sideEffects:false`, `exports` single `.` — ✅ lock-in free (see lock-in audit).

**Grade:** **B+** — correct Fetch-native surface, clean types, but 3 HIGH (decode 500, body consumption, proxy then) must ship before v0.1.

**Paths:** `packages/mino/src/{mino,router,context,compose,validator,client,sse,contract,errors,types}.ts`, `test/{mino,extra}.test.ts`

---

## 5. `@minostack/kernel` — `packages/kernel` — **B (architecture correct, P0 HIGH gaps)**

**Files:** 14 `src/*.ts` (token/scope/provider/container/module/decorators/application/lifecycle/execution-context/guards/exceptions/observability/dto/index).

### Findings by Dimension

| Dim                                            | Sev            | Finding                                                                                                                                                                                                                                                                                                                                 |
| ---------------------------------------------- | -------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Module `exports` ignored**                   | **HIGH**       | `module.ts:10-14` defines `exports?: Token[]` but `application.ts:87-106` registers **all** providers from all modules globally — no check if token is exported or re-exported. Breaks §21 strong boundaries.                                                                                                                           |
| **Guards/Pipes/Interceptors**                  | **HIGH**       | `decorators.ts:202` `@usePipes` + `guards.ts:21` `PipeTransform` defined, but `application.ts` never imports `runPipes` nor reads `__methodPipes`/`PIPES_META` → validation pipeline dead. Same for class-level `@useGuards`/`@useInterceptors` (`GUARDS_META`/`INTERCEPTORS_META` written never consumed, only `__methodGuards` read). |
| **Coverage gate**                              | **HIGH**       | `vitest.config.ts:4-9` overrides 90% → 50% to hide `kernel` 68% stmts / 55% branches (`application.ts:58`, `guards.ts:0%`, `dto.ts:58%`). Violates `AGENTS.md §9 Done means 90%`.                                                                                                                                                       |
| **Scopes eager**                               | **HIGH**       | `application.ts:138-158` eagerly `await container.get(tok)` for **all** tokens at bootstrap → `request` cached in `root.requestCache` (leak), `transient` instantiated once (should be per-get).                                                                                                                                        |
| **ExecutionContext `module` name**             | **HIGH**       | `application.ts:216-218` `module:{name: ctor.constructor.name}` → always `"Function"` (`ctor` is `Function`). Should be `(ctor as {name:string}).name`.                                                                                                                                                                                 |
| **Scope decorator ignored on raw `Container`** | **MEDIUM**     | `@injectable({scope:'transient'})` only honored inside `Application.init()` (`application.ts:92-102` reads `ctor.__scope`), direct `new Container().register(MyService)` stays `singleton`.                                                                                                                                             |
| **Reflect paramtypes not filtered**            | **MEDIUM**     | `container.ts:283-293` returns primitives (`String`, `Number`, `Object`, `Array`) as tokens → `No provider for String`.                                                                                                                                                                                                                 |
| **Lifecycle order not topological**            | **MEDIUM**     | `instances` order = module post-order + `allTokens` insertion, not dep topological — `onInit` may run before dep.                                                                                                                                                                                                                       |
| **ExceptionFilter misses `HttpError`**         | **MEDIUM**     | `exceptions.ts:47` checks `instanceof HttpException` only, but `mino` throws `HttpError` (different hierarchy) → 422 → 500 mis-map. Should duck-type `status`.                                                                                                                                                                          |
| **DTO duplicate silent**                       | **MEDIUM**     | `dto.ts:29` `DTO_REGISTRY.set(name,meta)` overwrites on duplicate name without error.                                                                                                                                                                                                                                                   |
| **Circular detection**                         | **LOW**        | Works via `seen` copying, but `private resolving Set` (`container.ts:33`) dead.                                                                                                                                                                                                                                                         |
| **Decorators lowercase**                       | **INFO**       | Provides `module`/`injectable`/`controller`/`get`/`post`... lowercase per spec, but re-exports uppercase aliases `Get`/`Post` etc. (`decorators.ts:143`) contradicts strict lowercase.                                                                                                                                                  |
| **Observability**                              | **LOW-MEDIUM** | OTel-compatible `Tracer`/`Span` skeleton correct, but `Instrumentation` (`onRequestStart/End`) never invoked, `createTraceContext` `Math.random` not `crypto.randomUUID`, global tracer no `AsyncLocalStorage`.                                                                                                                         |

**Correct:** DI discriminated union, deterministic DFS `visited` + `path.includes(mod)` cycle detection (`application.ts:63-68`), `Container` `has()` hierarchical, `override()`/`clearCache()` testability, `lifecycle.ts:38-55` forward `onInit`/`onStart`, reverse `onStop`/`onDestroy`, `sideEffects:false`.

**Grade:** **B** — direction `kernel→mino` correct, ESM, zero extra runtime deps, but 4 HIGH (exports, pipes, scopes eager, ExecutionContext bug) must be P0 before publish.

**Paths:** `packages/kernel/src/{application,container,module,decorators,lifecycle,execution-context,guards,lifecycle,exceptions,observability,dto}.ts`

---

## 6. `runtime-*: node / bun / deno` — `packages/runtime-*`

### `runtime-node` (`src/index.ts:248`) — Real streaming adapter, only complex adapter

| Sev        | Lines                              | Issue                                                                                                                                                                                                                                                                                                                                                                            |
| ---------- | ---------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **HIGH**   | `runtime-node/src/index.ts:94-113` | `Headers.forEach` coalesces `Set-Cookie` with `", "` — mangles `Expires=Thu, ...` and merges distinct cookies. Must use `(fetchRes.headers as any).getSetCookie?.()` then `nodeRes.setHeader("set-cookie", cookies)`; test `test/runtime.test.ts:81-108` never creates multi-cookie `Response` (fakes via two `Response`s + `mockRes.getHeader` override, `expect(anything())`). |
| **MEDIUM** | `src/index.ts:43-76`               | `Readable→ReadableStream` handles `data`/`end`/`error`/`close` but `close` (abort) incorrectly `controller.close()` not `error()`, no `Request.signal` wiring (`nodeReq.on("close",()=>ac.abort())`, `new Request(url,{signal:ac.signal})`).                                                                                                                                     |
| **MEDIUM** | `src/index.ts:121-143`             | `once("drain")` without `error`/`close` race → hang if client disconnects mid-stream.                                                                                                                                                                                                                                                                                            |
| **MEDIUM** | `src/index.ts:167-187`             | `createNodeRequestListener` `void (async()=>{...})` fire-and-forget, `console.error` unconditional noisy; `app.fetch` already catches handler errors (500 via `mino.ts:34`), this catch is catastrophic only.                                                                                                                                                                    |
| **MEDIUM** | `src/index.ts:203-228`             | `existingServer` double-listener: `createServer(handler)` already attaches, then `existingServer.on("request",handler)` duplicates. Dead `if(existingServer !== server)` (always false).                                                                                                                                                                                         |
| **HIGH**   | `vitest.config.ts:4-9`             | 90% → 60% override hides 77% stmts / 65% branches, uncovered `...136-137,178-183`.                                                                                                                                                                                                                                                                                               |

**Correct:** `duplex:"half"` (79-85), `Uint8Array` streaming, `set-cookie` append handling (intent correct, impl buggy), `signal`/`onListen` via `AbortController`, `engines.node>=22`.

### `runtime-bun` (`src/index.ts:70`, `test/bun.test.ts:3`) — Shim

- Correct: `Bun.serve({fetch: (req)=>app.fetch(req)})` zero conversion, preserves streaming/`getSetCookie`. **MEDIUM:** `BunServeOptions` lacks `signal`/`onListen` (Node/Deno have), silently ignores `AbortSignal.timeout()`. **MEDIUM:** `error` handler 500 fallback vs Deno missing. **LOW:** local `declare const Bun` subset, no `@types/bun`, `engines.node>=22` misleading (should be `bun>=1`). Coverage 70% stmts / 100% branches (mocked `globalThis.Bun` via `vi.fn`).

### `runtime-deno` (`src/index.ts:56`, `test/deno.test.ts:3`)

- Correct: `Deno.serve({port,hostname,signal,onListen}, (req)=>app.fetch(req))` + `signal`/`onListen` parity with Node. **LOW:** Missing `onError` vs Bun's `error`. **MEDIUM:** mocked `globalThis.Deno` tests only. **LOW:** `engines.node>=22` should be `deno`.

**Cross-cutting:**

| Theme                                                               | Sev        | Evidence                                                                   |
| ------------------------------------------------------------------- | ---------- | -------------------------------------------------------------------------- |
| Coverage gate 90→60 for all three                                   | **HIGH**   | `runtime-*/vitest.config.ts:4-9` vs `tooling/vitest-config/base.js:31-36`  |
| No real `toRequest` Body stream test (only `Readable.from(Buffer)`) | **MEDIUM** | `runtime-node/test/runtime.test.ts:27-46`                                  |
| `SSE`/`Uint8Array` binary not exercised via runtimes                | **MEDIUM** | No `c.sse()` integration test                                              |
| `Buffer→Uint8Array` copy per chunk (`new Uint8Array(chunk)`)        | **LOW**    | Could `enqueue(chunk)` zero-copy; Node 22 `Readable.toWeb` faster          |
| Lock-in minimal                                                     | ✅         | All `dependencies: {"@minostack/mino":"workspace:*"}`, `sideEffects:false` |

**Grade:** **B-** — `runtime-node` is real adapter with one **HIGH cookie bug** + abort/backpressure gaps; `bun`/`deno` are thin shims correct but under-tested and API-divergent on `signal`/`error`.

**Paths:** `packages/runtime-node/src/index.ts`, `packages/runtime-bun/src/index.ts`, `packages/runtime-deno/src/index.ts`, `test/*`, `package.json`

---

## 7. Tooling / Apps / Examples / Playground / Docs

| Area                                                                                                                                                                 | Status   | Evidence                                                                                                                                   |
| -------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- | ------------------------------------------------------------------------------------------------------------------------------------------ |
| `tooling/tsconfig` `base.json`/`library.json`                                                                                                                        | ✅       | `target ES2022, strict, NodeNext, stripInternal:true`                                                                                      |
| `tooling/eslint-config/base.js` `oxlint flat/recommended` + `typescript-eslint` + `prettier` last                                                                    | ✅       | `src/**/*.ts: no-restricted-imports node:*` (schema only) — should extend to `mino/kernel` `src` to enforce runtime-agnostic (§7.2).       |
| `tooling/vitest-config/base.js` 90% gate, `coverageConfigDefaults.exclude`                                                                                           | ✅       | But `defineWorkspace({exclude:["src/types.ts"]})` documented but not used in `graphql` (fragile).                                          |
| `apps/benchmark` `src/index.ts:105` micro-bench (`performance.now`, warm-up 1k, `bench("static")` etc.) + `pnpm --filter benchmark bench` 95k–132k req/s on Node 24  | ✅       | `package.json: private, scripts: bench/dev/build/typecheck/test/lint/clean` + `tsconfig.json` extends base                                 |
| `examples` `src/*.ts: basic-user,error-handling,graphql-sdl,openapi-document,recursive-category,refine-transform` + `test/examples.test.ts` (7 tests, 100% coverage) | ✅       | Compact, copy-paste, executed by tests (no rot)                                                                                            |
| `playground` `src/practicals/*: signup,blog,enterprise` + `proof.ts` `runPractical` `enterprise.openapi.json` 24k `enterprise.graphql` 1.6k + federated              | ✅       | Complex, not published, pinned snapshots                                                                                                   |
| `docs/architecture.md` `TypeScript stays on ^6` (7.1 stable API)                                                                                                     | ✅       |                                                                                                                                            |
| `.github/workflows/ci.yml` `check` → `build` + new `pack --dry-run` tarball guard (added 2026-09-08)                                                                 | ✅       | Prevents `mino` leaking `kernel`                                                                                                           |
| Root `README.md` updated to phi status with full roadmap table Phases 0–6 (added kernel, runtime, benchmark)                                                         | ✅       |                                                                                                                                            |
| `packages/mino/README.md` now documents `validator` with Zod/Valibot/ArkType snippets + `npm i @minostack/mino` zero lock-in                                         | ✅       |                                                                                                                                            |
| `LICENSE`                                                                                                                                                            | **TODO** | Still `TBD` (`README.md:136` “TBD — LICENSE will be added before public distribution”) — must add before `npm publish` per `AGENTS.md §6`. |

---

## 8. Cross-Cutting Security & Performance

| Concern                                                                                        | Status | Evidence                                                                                                                                                                                                                                                                                     |
| ---------------------------------------------------------------------------------------------- | ------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Prototype pollution** (`__proto__`)                                                          | ✅     | `schema` `setKey` `defineProperty`, `openapi` `setProp` for `properties`/`paths`/`components`, `graphql` `setField` `defineProperty` — tests `edges.test.ts:159-175`, `enterprise.test.ts:254-279`                                                                                           |
| **XSS / Header CRLF**                                                                          | ✅     | `context.ts:166` `Headers.set` relies on Fetch runtime throwing on `\r\n`; `HttpError` not reflecting user input.                                                                                                                                                                            |
| **Secret leakage** `HttpError expose: status<500`, `kernel defaultExceptionFilter` generic 500 | ✅     | `packages/mino/src/errors.ts:12` + `packages/kernel/src/exceptions.ts:47`                                                                                                                                                                                                                    |
| **DoS** large body                                                                             | ⚠️     | `runtime-node` streams, but `Mino` `compose` per-request allocs (see mino perf), `schema` `[...value]` per check — acceptable for v0.1.                                                                                                                                                      |
| **Performance claims** honest                                                                  | ✅     | No “zero-copy” marketing (runtime-node README says low-overhead), benchmark harness in `apps/benchmark` measures `Requests/sec, p50/p95/p99, RSS, heap, GC` per §31.                                                                                                                         |
| **GC pressure**                                                                                | ⚠️     | `mino`: `compose` closure per request (`mino.ts:288`), `context.ts:47` `new Headers()` per request + `finalizeHeaders` double copy + `body()` double `Response`; `kernel`: eager `transient`/`request` instantiation leaks. Documented as “correctness-first, optimize later” (user choice). |

---

## 9. Dependency Graph — Verified Correct

```
@minostack/schema (0 runtime deps, type:module, sideEffects:false)
   ↑
   ├─ @minostack/openapi (deps: schema only; dev: mino for tests)
   ├─ @minostack/graphql (deps: schema only)
   └─ @minostack/kernel (deps: schema + mino)
           ↑
         @minostack/mino (0 runtime deps)  ← required: zero lock-in ✅
           ↑
   @minostack/runtime-node|bun|deno (deps: mino only)
```

**No cycles, no `mino → kernel`, no `schema → mino`, no `openapi → mino` runtime, `runtime → kernel` — all per prompt §4.** `pnpm --filter ... pack --dry-run` confirms tarballs ship only `dist/` + `package.json` + `README.md`.

---

## 10. Prioritized Remediation (Top 15)

### P0 — Fix before v0.1 publish (HIGH, correctness/security)

1. **`runtime-node` `set-cookie` corruption** `packages/runtime-node/src/index.ts:94-113` → use `getSetCookie()` + test multi-cookie with `Expires=Thu,`. **File: `runtime-node/src/index.ts`**
2. **`mino/router` `decodeURIComponent` → 500** `packages/mino/src/router.ts:144,154` → `try/catch → 400` + `BadRequestError`. **File: `router.ts`**
3. **`mino/validator` body consumption** `packages/mino/src/validator.ts:68-73` → `clone()` + cache parsed JSON, `context.jsonBody()` read from `valid("json")` if present. **File: `validator.ts`**
4. **`mino/client` proxy `then` thenable** `packages/mino/src/client.ts:84-133` → `if(prop==="then"||prop==="catch"||prop==="finally") return undefined`. **File: `client.ts`**
5. **`kernel` `module.exports` ignored** `packages/kernel/src/application.ts:87-106` → track `providerOwner` + `exported` set, only register exported tokens globally. **File: `application.ts`**
6. **`kernel` `scope` eager leak** `packages/kernel/src/application.ts:138-158` → only `singleton` eager, `request`/`transient` lazy. **File: `application.ts`**
7. **`kernel` `ExecutionContext` module name `"Function"`** `packages/kernel/src/application.ts:216-218` → `ctor.name` not `ctor.constructor.name`. **File: `application.ts`**
8. **`kernel` pipes dead** `packages/kernel/src/application.ts` import `runPipes`, read `__methodPipes`/`PIPES_META`. **Files: `application.ts`, `decorators.ts:202`**

### P1 — Correctness & Gates (MEDIUM)

9. **`mino` path-scoped prefix bypass** `packages/mino/src/mino.ts:71` → `===prefix || startsWith(prefix+"/")`. **File: `mino.ts`**
10. **`mino` `HEAD` `content-length` mismatch** `packages/mino/src/mino.ts:293-296` → delete `content-length` or recompute. **File: `mino.ts`**
11. **`mino` `Types ParamsFor` optional `?`** `packages/mino/src/types.ts:7-17` → `?: string` when `?` present. **File: `types.ts`**
12. **Restore 90% gates** — remove `vitest.config.ts` overrides (mino 85/70, kernel 50, runtime 60) and add tests for `compose` double-continue, `router` wildcard non-last, `kernel` `exports`/`request` scope/`class guards`, `runtime-node` drain hang. **Files: `*/vitest.config.ts`**
13. **Wire `Request.signal` in `runtime-node`** `packages/runtime-node/src/index.ts:43-76` → `AbortController`, `nodeReq.on("close",()=>ac.abort())`, `new Request(url,{signal:ac.signal})`. **File: `runtime-node/src/index.ts`**
14. **`openapi` header sanitization** `packages/openapi/src/oas.ts:340-343` `headers/securitySchemes` via `setProp` loop. **File: `oas.ts`**

### P2 — Polish (LOW)

15. `mino` `SSE` `retry:0` (`src/sse.ts:16`), `context.ts:213` double `Response`, `router.ts: strict` flag unused, `kernel` `token.ts` `Map` leak, `graphql` `README` code list, `tooling/eslint-config` extend `no-restricted-imports` to `mino/kernel` `src`.

---

**Absolute paths for verification:**

- `/home/meaow/dev/tree/__bake__/minostack/packages/mino/src/{mino,router,context,compose,validator,client,sse,contract,errors,types}.ts`
- `/home/meaow/dev/tree/__bake__/minostack/packages/kernel/src/{application,container,module,decorators,lifecycle,execution-context,guards,lifecycle,exceptions,observability,dto}.ts`
- `/home/meaow/dev/tree/__bake__/minostack/packages/mino/test/{mino,extra}.test.ts`
- `/home/meaow/dev/tree/__bake__/minostack/packages/runtime-node/src/index.ts`, `test/runtime.test.ts`
- `/home/meaow/dev/tree/__bake__/minostack/packages/openapi/src/{oas,mino,types}.ts`, `test/mino.test.ts`
- `/home/meaow/dev/tree/__bake__/minostack/packages/graphql/src/{sdl,mino}.ts`, `test/mino.test.ts`
- `/home/meaow/dev/tree/__bake__/minostack/.github/workflows/ci.yml`, `pnpm-workspace.yaml`, `turbo.json`, `tooling/vitest-config/base.js`

**Overall Grade: A- (architecture exemplary, implementation B+; fix 8 P0 HIGH before publish, then P1 gates).** _No files were modified — read-only review._
